<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lii.demo.example.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.lii.demo.example.entity.po.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="pass_word" property="passWord" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="uuid" property="uuid" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 查询条件 -->
    <sql id="Base_Column_List">
        id,user_name,pass_word,phone,status,uuid
    </sql>
    <!-- 筛选条件 -->
    <sql id="Base_Where_Condition">
        <where>
            <if test="id != 0">
                AND id = #{id,jdbcType=BIGINT}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name = #{userName,jdbcType=VARCHAR}
            </if>
            <if test="passWord != null and passWord != ''">
                AND pass_word = #{passWord,jdbcType=VARCHAR}
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone,jdbcType=VARCHAR}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status,jdbcType=VARCHAR}
            </if>
            <if test="uuid != null and uuid != ''">
                AND uuid = #{uuid,jdbcType=VARCHAR}
            </if>
        </where>
    </sql>
    <select id="selectUser" resultMap="BaseResultMap" parameterType="com.lii.demo.example.entity.po.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM test_user
        <include refid="Base_Where_Condition"/>
    </select>

</mapper>